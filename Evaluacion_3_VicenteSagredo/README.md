-- Cosas importantes para la evaluacion -- 
En caso de que no deje instalar Set-ExecutionPolicy Unrestricted: para activar la opcion activar ejecucion de sqripts

- winget install OpenJS.NodeJS.LTS

- npm install -g pnpm

- Inicializar el proyecto
pnpm create vite@latest mi-landing -- --template react
cd mi-landing
pnpm install

- Levantar el servidor de desarrollo
pnpm dev